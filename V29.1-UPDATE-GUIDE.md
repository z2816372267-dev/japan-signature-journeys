# 飞鸟旅行 V29.1 更新操作

V29.1 修复第 1 天和第 6 天接送机的“车程 / 耗时”排版。羽田机场、成田机场分别独立成行，距离和时间不会再被拆成不规整的两行；电脑官网、手机官网、统一后台编辑器和后台实时预览使用同一套内容。

## 一、上传网站文件到 GitHub

1. 下载并解压 `asuka-travel-v29.1-github-upload.zip`。
2. 打开 GitHub 上传页：`https://github.com/z2816372267-dev/japan-signature-journeys/upload/main`。
3. 把解压后看到的全部文件和文件夹拖入网页。不要直接上传 ZIP 文件。
4. 等待文件列表全部读取完成。
5. 页面下方的提交说明填写：`发布V29.1接送机车程排版修复`。
6. 点击绿色提交按钮，等待 GitHub 完成更新。

## 二、更新现有 CloudBase 云函数

这一步用于让后台读取旧草稿时自动整理旧格式，并保证以后从后台发布的官网仍保持新排版。

1. 打开腾讯云 CloudBase 控制台，进入环境 `asuka-travel-admin-d6cef3d2ad7da`。
2. 左侧点击“云函数 / 托管”，进入现有函数 `asuka-cms`。不要新建函数。
3. 进入“函数代码”，选择本地上传 ZIP。
4. 上传 `asuka-cms-v29.1-cloud-function.zip`。压缩包第一层应直接看到 `index.js`、`package.json` 和 `lib` 文件夹。
5. 点击“保存并安装依赖”，等待页面提示更新完成。
6. 回到“函数配置”，确认执行超时仍为 `60 秒`、公网访问仍开启，原来的环境变量全部保留。

这次不需要新建数据库集合、不需要新增安全域名，也不需要重新生成 GitHub 令牌。

## 三、确认更新结果

1. GitHub 提交完成后等待约 1—3 分钟。
2. 打开后台，在左下角确认版本显示为 `V29.1 · 接送机排版修复`。
3. 进入“每日行程”，查看第 1 天和第 6 天：羽田、成田两条路线应分别占一行。
4. 点击“保存草稿”，再到“发布管理”发布官网。
5. 打开最新版官网，分别用电脑和手机检查“车程 / 耗时”卡片。

如果后台首次打开时仍看到旧的一行文字，刷新一次页面；V29.1 云函数会在读取草稿时自动转换旧格式。
