# 飞鸟旅行 V32 更新说明

V32 把官网首页内容接入现有飞鸟旅行后台。电脑和手机仍使用同一个后台、同一套 CloudBase 账号、同一个 `asuka-cms` 云函数和同一个“发布官网”按钮。

## V32 新增内容

- 左侧新增“01 首页内容”。
- 可编辑3张封面轮播图、封面文字和两个按钮文字。
- 可编辑品牌介绍。
- 可编辑6项“飞鸟之选”的图片、地区、标题、说明与按钮文字。
- 可编辑3种出发方式的图片、标题、说明与按钮文字。
- 右侧继续支持桌面／手机实时预览。
- 首页和关东6日行程使用独立草稿、独立版本号和独立发布记录，不会互相覆盖。
- 版式、颜色、动画、关东详情入口和三种出发方式按钮去向保持锁定。
- 不需要新增数据库集合，继续使用现有6个集合。

## 第一步：上传 V32 官网文件到 GitHub

1. 下载并解压 `飞鸟旅行-V32-GitHub上传包.zip`。
2. 打开：
   `https://github.com/z2816372267-dev/japan-signature-journeys/upload/main`
3. 把解压后文件夹里面的全部文件和文件夹拖入 GitHub 上传区域。
   - 要拖入的是 `index.html`、`admin`、`admin-src`、`content`、`cloudbase` 等内容。
   - 不要只上传最外层的“飞鸟旅行-V32-GitHub上传包”文件夹。
4. 等文件上传完成。
5. 在提交说明第一行填写：
   `V32：统一后台管理官网首页`
6. 选择直接提交到 `main`。
7. 点击绿色提交按钮。
8. 等待 GitHub Pages 更新，通常需要1—3分钟。

## 第二步：把 V32 云函数包部署到现有 asuka-cms

网站文件提交成功后再执行本步骤。

1. 登录腾讯云 CloudBase。
2. 确认左上角环境是：
   `asuka-travel-admin-d6cef3d2ad7da`
3. 左侧点击“云函数／托管”。
4. 在函数列表中点击现有函数：
   `asuka-cms`
5. 点击上方“函数代码”。
6. 提交方法选择“前端编辑器”。
7. 点击编辑器上方的“文件”或“…”菜单，选择上传 ZIP／上传函数代码。
8. 选择 `飞鸟旅行-V32-CloudBase云函数包.zip`。
9. 如果出现“覆盖确认”，点击“确认”。
   - 这里应该覆盖当前工作区代码，因为 V32 包含完整的最新版函数。
   - 不会删除环境变量、数据库集合或登录设置。
10. 上传结束后，确认编辑器根目录直接看到：
    - `index.js`
    - `package.json`
    - `lib`
11. 点击“保存并安装依赖”。
12. 等待页面提示保存／部署完成。

## 第三步：检查函数配置

进入 `asuka-cms` 的“函数配置”，确认：

- 内存：`256M`
- 执行超时：`60秒`
- 公网访问：开启
- 现有环境变量全部保留，不要删除或重新填写。

V32 不需要新增环境变量，也不需要新增数据库集合。

## 第四步：检查 V32 后台

1. 打开：
   `https://z2816372267-dev.github.io/japan-signature-journeys/admin/?v=32`
2. 使用原来的工作邮箱登录。
3. 左侧第一项应显示“01 首页内容”。
4. 依次检查：
   - 首页封面
   - 品牌介绍
   - 飞鸟之选
   - 三种出发方式
5. 点击右上角“桌面”和“手机”，确认右侧预览跟随切换。
6. 再点击“产品概览”和“每日行程”，确认原有关东6日内容仍在。

## 第五步：第一次首页发布

1. 在“首页内容”中完成修改。
2. 点击“保存草稿”。
3. 确认出现“首页草稿已安全保存”。
4. 点击“发布官网”。
5. 提交说明可填写：
   `更新飞鸟旅行官网首页内容`
6. 点击“确认发布官网”。
7. 等待返回 GitHub 版本地址和最新版官网地址。

## 常见情况

### 后台提示“云函数尚未支持官网首页”

说明 GitHub 网站文件已经上传，但 V32 云函数包还没有成功部署。重新执行“第二步”，并点击“保存并安装依赖”。

### 首页已发布，但官网暂时没变化

GitHub Pages 通常需要1—3分钟。打开带版本参数的官网地址，或强制刷新浏览器。

### 是否需要新建第7个集合

不需要。V32 的首页草稿继续写入现有 `cms_drafts`，但文档编号使用 `homepage`，与关东行程 `kanto-6d` 分开保存。
