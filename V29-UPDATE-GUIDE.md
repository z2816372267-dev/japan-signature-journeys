# 飞鸟旅行 V29 更新操作

V29 继续使用现在这一个后台。电脑和手机打开同一网址、使用同一批账号和同一份数据，不需要建立第二个后台或第二个 CloudBase 环境。

## 第一部分：上传网站文件到 GitHub

1. 下载并解压 `asuka-travel-v29-github-upload.zip`。
2. 打开 GitHub 上传页：`https://github.com/z2816372267-dev/japan-signature-journeys/upload/main`。
3. 把解压后看到的全部文件和文件夹拖进网页。不要直接上传 ZIP 文件。
4. 等待文件列表全部显示完成。
5. 在页面下方提交说明填写：`发布V29统一后台与手机端优化`。
6. 点击绿色的提交按钮，等待 GitHub 完成。

## 第二部分：重新部署 CloudBase 云函数

网站上传完成后，还必须更新现有的 `asuka-cms` 云函数，否则多人保护、可靠发布和工作人员管理不会生效。

1. 打开腾讯云 CloudBase 控制台。
2. 选择环境 `asuka-travel-admin-d6cef3d2ad7da`。
3. 左侧点击“云函数 / 托管”。
4. 点击现有函数 `asuka-cms`，不要新建第二个函数。
5. 进入函数代码或代码管理页面，选择本地上传 ZIP。
6. 上传 `asuka-cms-v29-cloud-function.zip`；ZIP 打开后第一层应直接看到 `index.js`、`package.json` 和 `lib` 文件夹。
7. 选择安装依赖或在线安装依赖，然后点击部署/保存并部署。
8. 部署成功后进入函数配置，核对超时时间实际显示为 `60 秒`，运行环境为 Node.js 20，执行方法为 `index.main`。
9. 原有环境变量全部保留，不需要重新生成 GitHub 令牌，也不要删除现有 6 个数据库集合。

如果发布时仍出现 `FUNCTIONS_TIME_LIMIT_EXCEEDED` 或 `3 seconds`，表示线上函数仍是 3 秒配置，需要再次把超时时间改为 60 秒并重新部署。

## 第三部分：确认更新成功

1. 等待 GitHub Pages 约 1—3 分钟。
2. 打开后台，在左下角确认出现 `V29 · 电脑与手机统一后台`。
3. 用手机打开同一个后台网址，确认顶部仍可看到“保存草稿”。
4. 修改一小段文字并保存草稿。
5. 进入“发布管理”，点击发布官网。
6. 打开最新版官网，检查手机菜单和第 1 天羽田/成田车程信息。

V29 不新增安全域名，不新增数据库集合，也不改变现有工作人员的登录邮箱。
