# 飞鸟旅行后台 V34.2 更新说明

本版本以 GitHub `main` 最新提交 `1f344144b9b21fc125074cc8ecb146da5bdf9b68` 为唯一基准，统一修复逐日动线编辑、草稿冲突恢复、发布流程和独立行程页 404。

## 本次改动

- 行程地图中的 DAY 01、DAY 02 等内容明确来自“每日行程”的当天标题与当天路线，并可直接跳转到指定日期编辑。
- 草稿冲突改为直接读取 CloudBase 数据库，不再尝试读取尚未发布的 GitHub 页面；读取前会保留本机恢复副本。
- 发布中心改成固定两步：先“保存后台草稿”，再“检查并发布到官网”。
- 有未保存内容时，发布按钮会锁定；草稿保存成功后才可发布。
- 新行程尚未生成独立页面时，不再显示会跳转到 404 的官网链接。
- 完整网站包补齐 `journeys/` 目录、关东6日独立页面、共用样式和脚本。
- 官网与独立行程页继续使用本地静态文件、响应式图片和手机端单列排版，不新增境外第三方加载资源。

## 第一步：上传 Windows 覆盖更新包到 GitHub

1. 解压 `Asuka-Travel-V34.2-Windows-GitHub-Update.zip`。此包只包含 V34.2 必须新增或覆盖的文件，不重复打包仓库中已有的图片，因此体积更小。
2. 打开：
   <https://github.com/z2816372267-dev/japan-signature-journeys/upload/main>
3. 将解压后的全部文件和文件夹拖入上传区域；原仓库中的其他文件保持不动，旧版后台哈希文件无需手动删除。提交说明建议填写：
   `V34.2 修复动线、草稿、发布流程与独立行程页`
4. 等待 GitHub Pages 更新约 1—3 分钟。

当前官网和后台都由 GitHub Pages 提供；完成这一步后，不需要再为这两个网址另外配置“静态托管”。

## 第二步：更新现有 asuka-cms 云函数

草稿冲突修复依赖新版云函数，因此网站上传后还要执行此步骤：

1. 打开腾讯云 CloudBase 控制台，进入现有环境 `asuka-travel-admin-d6cef3d2ad7da`。
2. 进入“云函数”，打开现有的 `asuka-cms`；不要新建同名函数，也不要删除原函数。
3. 选择更新函数代码，上传 `Asuka-Travel-V34.2-Windows-CloudBase-Function.zip`。
4. 开启按 `package.json` 自动安装依赖并部署。
5. 核对运行环境为 Node.js 20、执行方法为 `index.main`、超时时间为 60 秒。

重新部署代码不会删除现有账号、草稿、图片或环境变量。

## 两个按钮的准确作用

1. “保存后台草稿”：只保存编辑进度，游客看不到，官网不会改变。
2. “检查并发布到官网”：生成静态行程页、更新首页行程目录并提交 GitHub，游客之后会看到新版本。
3. “打开已发布页面”：只在系统确认页面已经存在时出现。

## 上线后检查

- 官网：<https://z2816372267-dev.github.io/japan-signature-journeys/>
- 后台：<https://z2816372267-dev.github.io/japan-signature-journeys/admin/>
- 关东6日独立页：<https://z2816372267-dev.github.io/japan-signature-journeys/journeys/kanto-6d/>

上传后使用 `Ctrl + F5` 强制刷新后台。进入“预览与发布”时，应看到第1步和第2步；编辑内容后，第2步应保持锁定，直到第1步保存成功。

## 验证结果

- 自动测试：32 项全部通过。
- 内容、图片路径与静态生成检查：通过。
- 后台生产构建：通过。
- 关东6日独立页面、共用 CSS/JS 和站点地图：已生成并包含在上传包内。
